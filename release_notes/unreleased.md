**Unreleased**

* Reject exact and nested-encoded dot segments in Microsoft Graph path identifiers.
* Require the pending-flow nonce before redirecting a browser into an OAuth flow.
* Process every polling mode in lossless chronological order and retry emails whose artifact ingestion previously failed.

* Bound nested item-attachment extraction and propagate depth-limit failures.
