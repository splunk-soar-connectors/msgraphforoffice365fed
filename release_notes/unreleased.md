**Unreleased**

* Encode caller-controlled Microsoft Graph path segments.
* Restrict pagination to the configured Graph origin and bound page traversal.
* Bind OAuth callbacks to their initiating authorization flow with a single-use nonce.
